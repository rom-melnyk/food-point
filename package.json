{
  "name": "food-point",
  "version": "0.0.1",
  "description": "Food Point site",
  "main": "server.es6",
  "config": {
    "port": 8080,
    "mysql": {
      "host": "localhost",
      "user": "root",
      "password": "",
      "database": "food_point"
    }
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev-api": "nodemon --watch api --exec \"node --harmony --use_strict\" ./server.es6",
    "dev-static": "watchify src/app.es6 -t babelify -o static/script.dev.js -d -v",
    "dev-style": "node-sass -w --source-map-embed --output-style=expanded src/style.scss static/css/style.dev.css"
  },
  "author": "Roman Melnyk",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.3",
    "mysql": "^2.9.0"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.1.18",
    "babelify": "^7.2.0",
    "body-parser": "^1.14.1",
    "browserify": "^12.0.1",
    "mustache": "^2.2.0",
    "node-sass": "^3.4.2",
    "nodemon": "^1.8.1",
    "uglify": "^0.1.5",
    "watchify": "^3.6.0"
  }
}
