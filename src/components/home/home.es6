import React from 'react';

export default React.createClass({
    render () {
        return (
            <div className="view home">
                Йо!
                То є домашня сторінка.
            </div>
        );
    }
});
